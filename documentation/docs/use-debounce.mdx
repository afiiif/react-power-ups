import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

import Param from '@site/src/components/param';
import BrowserWindow from '@site/src/components/browser-window';

import { DemoDebounce, DemoThrottle } from '@site/src/examples/use-debounce-throttle';

# useDebounce

Get debounced value.

<hr className="custom-hr" />

## API

```js
const debouncedValue = useDebounce(value, delay, callbackFn);
```

### Options

- <Param name="value" type="any (T)" isRequired />

  - The value to be debounced

- <Param name="delay" type="number" isRequired />

  - Delay in miliseconds

- <Param name="callbackFn" type="(value: T) => void" isRequired={false} />

  - Callback function after debounced

### Returns

- <Param name="debouncedValue" type="T (generic)" />

  - The debounced value

<hr className="custom-hr" />

## Examples

<BrowserWindow>
  <DemoDebounce />
  <div className="h-4" />
  <DemoThrottle />
</BrowserWindow>

<Tabs>
<TabItem value="JS" label="JS">

```jsx
import { useState } from 'react';
import { useDebounce, useThrottle } from 'react-power-ups';

export function DemoDebounce() {
  const [keyword, setKeyword] = useState('');
  const debouncedKeyword = useDebounce(keyword, 1000);

  return (
    <>
      <h3>Debounce 1 second</h3>
      <input type="text" value={keyword} onChange={(e) => setKeyword(e.target.value)} />
      <div>Keyword: {keyword}</div>
      <div>Debounced keyword: {debouncedKeyword}</div>
    </>
  );
}

export function DemoThrottle() {
  const [keyword, setKeyword] = useState('');
  const throttledKeyword = useThrottle(keyword, 1000);

  return (
    <>
      <h3>Throttle 1 second</h3>
      <input type="text" value={keyword} onChange={(e) => setKeyword(e.target.value)} />
      <div>Keyword: {keyword}</div>
      <div>Throttled keyword: {throttledKeyword}</div>
    </>
  );
}
```

</TabItem>
<TabItem value="TS" label="TS" default>

```jsx
import { useState } from 'react';
import { useDebounce, useThrottle } from 'react-power-ups';

export function DemoDebounce() {
  const [keyword, setKeyword] = useState('');
  const debouncedKeyword = useDebounce(keyword, 1000);

  return (
    <>
      <h3>Debounce 1 second</h3>
      <input type="text" value={keyword} onChange={(e) => setKeyword(e.target.value)} />
      <div>Keyword: {keyword}</div>
      <div>Debounced keyword: {debouncedKeyword}</div>
    </>
  );
}

export function DemoThrottle() {
  const [keyword, setKeyword] = useState('');
  const throttledKeyword = useThrottle(keyword, 1000);

  return (
    <>
      <h3>Throttle 1 second</h3>
      <input type="text" value={keyword} onChange={(e) => setKeyword(e.target.value)} />
      <div>Keyword: {keyword}</div>
      <div>Throttled keyword: {throttledKeyword}</div>
    </>
  );
}
```

</TabItem>
</Tabs>

<hr className="custom-hr" />

Code: https://github.com/afiiif/react-power-ups/blob/main/src/use-debounce.ts
